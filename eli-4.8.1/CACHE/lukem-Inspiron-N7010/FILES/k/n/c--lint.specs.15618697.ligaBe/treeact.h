

#define _TERMACT_rule_15

#define _TERMACT_rule_14

#define _TERMACT_rule_13

#define _TERMACT_rule_12

#define _TERMACT_rule_11

#define _TERMACT_rule_10

#define _TERMACT_rule_9

#define _TERMACT_rule_8

#define _TERMACT_rule_7

#define _TERMACT_rule_6

#define _TERMACT_rule_5

#define _TERMACT_rule_4

#define _TERMACT_rule_3

#define _TERMACT_rule_2

#define _TERMACT_rule_1

#define _TERMACT_MonadicExp

#define _TERMACT_DyadicExp

#define _TERMACT_IdnVal

#define _TERMACT_FloatVal

#define _TERMACT_IntVal

#define _TERMACT_FloatParDecl

#define _TERMACT_IntParDecl

#define _TERMACT_Initialize

#define _TERMACT_VarDecl

#define _TERMACT_FloatVar

#define _TERMACT_IntVar

#define _TERMACT_UMinusOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PercentOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_SlashOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_StarOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_MinusOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PlusOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_GreaterOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LessOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EqualOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ExclOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NotExp

#define _TERMACT_AmpOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BarOp \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ShortCircuitExp

#define _TERMACT_CondExp

#define _TERMACT_AssignExp

#define _TERMACT_ExprStmt

#define _TERMACT_FloatResult

#define _TERMACT_IntResult

#define _TERMACT_Source

#define _TERMACT_ReturnStmt \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ContinueStmt \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BreakStmt \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_Loop

#define _TERMACT_TwoSided

#define _TERMACT_OneSided

#define _TERMACT_AppliedOccurrence \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DefiningOccurrence \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);
